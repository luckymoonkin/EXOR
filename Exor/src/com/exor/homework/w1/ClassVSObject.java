package com.exor.homework.w1;

public class ClassVSObject {
	/*
	 * class = 객체를 만들기 위한 설계도
	 * object(instance) = class를 기반으로 생성된 객체
	 * 
	 * 메모리 관점에서 차이점
	 * - class의 코드는 메모리의 '메소드 영역'에 로드되며, 한 번 로드 된 후에는 프로세스 종료 시까지 유지되어 해당 클래스로 객체를 생성할 때마다
	 * 	 다시 로드할 필요 없이 해당 메모리 위치를 참조
	 * - object는 메모리의 '힙 영역'에 할당되는데, 각 객체는 개별적인 메모리 공간에서 독립적으로 존재하며, 참조되지 않으면 가비지 컬렉터에게 정리당함
	 */
}
